<UserControl x:Class="MyToDo.Views.MsgView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyToDo.Views"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d" 
             Height="300" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" d:Text="温馨提示" FontSize="20" FontWeight="Black" Margin="12,5"/>
        <TextBlock Text="{Binding Message}" d:Text="确认删除吗？" 
                   FontSize="20" FontWeight="Black" Margin="12,5" Grid.Row="1"
                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="确定" Command="{Binding SaveCommand}" FontSize="18" Margin="0,0,30,0"/>
            <Button Content="取消" Command="{Binding CancelCommand}" FontSize="18"/>
        </StackPanel>
    </Grid>
</UserControl>
