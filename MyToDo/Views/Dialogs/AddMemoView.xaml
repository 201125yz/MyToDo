<UserControl x:Class="MyToDo.Views.Dialogs.AddMemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyToDo.Views.Dialogs"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="添加备忘录" FontSize="20" FontWeight="Black" Margin="12,5"/>
        <DockPanel LastChildFill="False" Grid.Row="1">
            <!--<StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                <TextBlock Text="状态:" FontSize="18" FontWeight="Black" Margin="12,5"/>
                <ComboBox Width="60">
                    <ComboBoxItem>待办</ComboBoxItem>
                    <ComboBoxItem>已完成</ComboBoxItem>
                </ComboBox>
            </StackPanel>-->
            <StackPanel >
                <TextBox md:HintAssist.Hint="请输入备忘录概要" FontSize="18" Margin="20,10" 
                         md:TextFieldAssist.HasClearButton="True" Width="200"
                         Text="{Binding Model.Title}"/>
                <TextBox md:HintAssist.Hint="请输入备忘录内容" FontSize="18" Margin="20,10" 
                     md:TextFieldAssist.HasClearButton="True" Width="200"
                     MinHeight="100" TextWrapping="Wrap" 
                         Text="{Binding Model.Content}"/>
            </StackPanel>
        </DockPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="确定" Command="{Binding SaveCommand}" FontSize="18" Margin="0,0,30,0"/>
            <Button Content="取消" Command="{Binding CancelCommand}" FontSize="18"/>
        </StackPanel>
    </Grid>
</UserControl>
